content:
  application/json:
    schema:
      properties:
        data:
          $ref: '../schemas/FederatedActivity.yaml'
        meta:
          $ref: '../schemas/MetaTotalPages.yaml'
      type: object
description: The request was successful.
