description: Retrieve activities from the specified network(s).
in: query
name: network
required: false
schema:
  items:
    $ref: "../../schemas/Network.yaml"
  type: array
  x-go-type-skip-optional-pointer: true